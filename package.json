{
  "name": "skysfalling-website",
  "private": true,
  "scripts": {
    "start": "cd shared && npm run start && cd ../client && npm run start",
    "build": "cd shared && npm run build && cd ../client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^4.9.5",
    "date-fns": "^2.30.0"
  }
}
