{
  "name": "skysfalling-website",
  "private": true,
  "scripts": {
    "start:server": "cd server && npm run start",
    "start:client": "cd client && npm run start",
    "build:server": "cd shared && npm run install-deps && npm run build && cd ../server && npm run build",
    "build:client": "cd shared && npm run install-deps && npm run build && cd ../client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^4.9.5",
    "date-fns": "^2.30.0"
  }
}
